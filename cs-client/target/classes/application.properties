# defaults (override with env vars)
cs.app.key=${CS_APP_KEY:}
cs.app.sec=${CS_APP_SEC:}
cs.callback.url=${CS_CALLBACK_URL:https://127.0.0.1}
cs.tokens.file=${CS_TOKENS_FILE:tokens.json}
cs.refresh.interval.ms=${CS_REFRESH_INTERVAL_MS:1800000}

# Note: MicroProfile mapping: CS_APP_KEY → cs.app.key (underscores map to dots, lowercase).